import { c as ModuleReference } from './Registry.types-tb_Smu0e.js';

/**
 * The variant names that a type supports.
 */
type VariantKeys<T> = T extends T ? keyof T : never;

type ModuleType = VariantKeys<ModuleReference>;

export type { ModuleType as M };
