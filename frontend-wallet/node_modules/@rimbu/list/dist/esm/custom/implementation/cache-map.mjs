export class CacheMap {
    constructor() {
        this.map = new Map();
    }
    get(key) {
        return this.map.get(key);
    }
    setAndReturn(key, value) {
        this.map.set(key, value);
        return value;
    }
}
//# sourceMappingURL=cache-map.mjs.map